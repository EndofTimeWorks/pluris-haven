{
  "name": "pluris-haven-tooling",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "prepare": "husky",
    "format": "git ls-files -z | xargs -0 prettier --write --ignore-unknown",
    "format:check": "git ls-files -z | xargs -0 prettier --check --ignore-unknown",
    "check:l10n": "node scripts/check-localisations.mjs",
    "check:ui-l10n": "node scripts/check-ui-localisations.mjs",
    "ci:local": "scripts/ci-local.sh",
    "lint:website": "pnpm --dir website check",
    "lint:server:format": "server/.venv/bin/ruff format --check server/pluris_server server/tests",
    "lint:server": "server/.venv/bin/ruff check server/pluris_server server/tests",
    "lint:shell": "shellcheck scripts/*.sh server/deploy/*.sh",
    "lint:mobile": "cd mobile && dart format --set-exit-if-changed lib test && flutter analyze",
    "test:server": "server/.venv/bin/pytest -q server/tests",
    "test:mobile": "cd mobile && flutter test",
    "test:debug-release-version": "node --test scripts/debug-release-version.test.mjs",
    "test:release": "node --test scripts/release-marker.test.mjs scripts/release-candidate.test.mjs scripts/generate-release-notes.test.mjs scripts/check-mobile-release-version.test.mjs scripts/official-release-policy.test.mjs scripts/play-generated-apk.test.mjs scripts/obtainium-config.test.mjs",
    "lint": "pnpm format:check && pnpm check:l10n && pnpm check:ui-l10n && pnpm lint:website && pnpm lint:server:format && pnpm lint:server && pnpm lint:shell",
    "commitlint": "commitlint --from=HEAD~1 --to=HEAD --verbose"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2",
    "husky": "^9.1.7",
    "prettier": "^3.9.7",
    "prettier-plugin-svelte": "^4.1.1"
  }
}
