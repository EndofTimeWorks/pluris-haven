# Pluris Haven Mobile

Flutter app.

## Run

```sh
flutter analyze
flutter test
flutter run
```

From the repo root, use the pinned SDK:

```sh
mise trust
mise install
cd mobile
flutter run
```

Open this directory in Android Studio.
